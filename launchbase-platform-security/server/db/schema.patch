diff --git a/server/db/schema.ts b/server/db/schema.ts
index 7713f8b..d374797 100644
--- a/server/db/schema.ts
+++ b/server/db/schema.ts
@@ -1762,10 +1762,12 @@ export const agentRuns = mysqlTable("agent_runs", {
     riskTier: number;
   }>(),
   approvedAt: timestamp("approvedAt"),
+  projectId: varchar("projectId", { length: 36 }), // FK to projects.id
 }, (table) => ({
   createdByIdx: index("agent_runs_createdBy_idx").on(table.createdBy),
   statusIdx: index("agent_runs_status_idx").on(table.status),
   createdAtIdx: index("agent_runs_createdAt_idx").on(table.createdAt),
+  projectIdIdx: index("agent_runs_projectId_idx").on(table.projectId),
 }));
 export type AgentRun = typeof agentRuns.$inferSelect;
 export type InsertAgentRun = typeof agentRuns.$inferInsert;
@@ -1796,6 +1798,68 @@ export const agentEvents = mysqlTable("agent_events", {
 export type AgentEvent = typeof agentEvents.$inferSelect;
 export type InsertAgentEvent = typeof agentEvents.$inferInsert;

+/**
+ * Agent artifacts (screenshots, logs, traces, reports, files, videos)
+ * Linked to a specific agent run
+ */
+export const agentArtifacts = mysqlTable("agent_artifacts", {
+  id: varchar("id", { length: 36 }).primaryKey(), // UUID
+  runId: int("runId").notNull(), // FK to agent_runs
+  type: mysqlEnum("type", ["screenshot", "log", "trace", "report", "file", "video"]).notNull(),
+  path: varchar("path", { length: 512 }).notNull(), // Local path or s3://bucket/key
+  filename: varchar("filename", { length: 255 }),
+  mimeType: varchar("mimeType", { length: 128 }),
+  sizeBytes: int("sizeBytes"),
+  metadata: json("metadata").$type<Record<string, unknown>>(),
+  createdAt: timestamp("createdAt").defaultNow().notNull(),
+}, (table) => ({
+  runIdIdx: index("agent_artifacts_runId_idx").on(table.runId),
+  typeIdx: index("agent_artifacts_type_idx").on(table.type),
+  createdAtIdx: index("agent_artifacts_createdAt_idx").on(table.createdAt),
+}));
+export type AgentArtifact = typeof agentArtifacts.$inferSelect;
+export type InsertAgentArtifact = typeof agentArtifacts.$inferInsert;
+
+// ============================================================================
+// PROJECTS & COLLABORATION
+// ============================================================================
+
+/**
+ * Projects — top-level container for runs, artifacts, and access control
+ */
+export const projects = mysqlTable("projects", {
+  id: varchar("id", { length: 36 }).primaryKey(), // UUID
+  name: varchar("name", { length: 255 }).notNull(),
+  description: text("description"),
+  createdBy: int("createdBy").notNull(), // FK to users.id
+  status: mysqlEnum("status", ["active", "archived"]).default("active").notNull(),
+  createdAt: timestamp("createdAt").defaultNow().notNull(),
+  updatedAt: timestamp("updatedAt").defaultNow().onUpdateNow().notNull(),
+}, (table) => ({
+  createdByIdx: index("projects_createdBy_idx").on(table.createdBy),
+  statusIdx: index("projects_status_idx").on(table.status),
+}));
+export type Project = typeof projects.$inferSelect;
+export type InsertProject = typeof projects.$inferInsert;
+
+/**
+ * Project collaborators — grants non-owner users access to a project
+ */
+export const projectCollaborators = mysqlTable("project_collaborators", {
+  id: int("id").autoincrement().primaryKey(),
+  projectId: varchar("projectId", { length: 36 }).notNull(), // FK to projects.id
+  userId: int("userId").notNull(), // FK to users.id
+  role: mysqlEnum("role", ["viewer", "editor", "admin"]).default("viewer").notNull(),
+  status: mysqlEnum("status", ["active", "revoked"]).default("active").notNull(),
+  createdAt: timestamp("createdAt").defaultNow().notNull(),
+}, (table) => ({
+  projectIdIdx: index("pc_projectId_idx").on(table.projectId),
+  userIdIdx: index("pc_userId_idx").on(table.userId),
+  projectUserIdx: uniqueIndex("pc_project_user_idx").on(table.projectId, table.userId),
+}));
+export type ProjectCollaborator = typeof projectCollaborators.$inferSelect;
+export type InsertProjectCollaborator = typeof projectCollaborators.$inferInsert;
+
 // ============================================================================

 // ============================================================================
